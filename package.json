{
  "name": "talk-with-jamie",
  "version": "1.0.0",
  "private": true,
  "description": "Netlify-hosted AI chat with accounts, admin drafting tools, and persistent conversations",
  "scripts": {
    "build": "node tools/guard-production.js && npm test",
    "check:js": "node --check netlify/functions/_lib.js && node --check netlify/functions/api.js",
    "check:site": "node tools/check-site.js",
    "test:unit": "node --test test/*.test.js",
    "test": "npm run check:js && npm run check:site && npm run test:unit"
  },
  "dependencies": {
    "@netlify/blobs": "^10.7.9"
  },
  "engines": {
    "node": ">=20"
  }
}
